<!-- Cliente Section -->
<div class="mb-6 bg-white p-6 rounded-lg shadow-md">
  <h2 class="text-lg font-semibold text-gray-900 mb-4 border-b pb-2">Gestión de Cliente</h2>
  <div class="space-y-4">
    <div class="flex items-center justify-between">
      <input type="text" [(ngModel)]="dni" placeholder="Ingrese DNI del cliente" class="mt-1 block w-2/3 py-2 px-3 text-base border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-md shadow-sm" />
      <button (click)="buscarCliente()" class="bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700 transition duration-200 shadow-md">
        Buscar Cliente
      </button>
    </div>

    <!-- Si no se encuentra el cliente, mostrar mensaje -->
    <div *ngIf="clienteNoEncontrado" class="mt-4 p-4 bg-red-100 text-red-700 border border-red-400 rounded-md">
      <p>No se ha encontrado ningún cliente con ese DNI. ¿Desea agregar uno nuevo?</p>
      <button (click)="abrirAgregarCliente()" class="mt-2 bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700">
        Sí, agregar cliente
      </button>
    </div>

    <!-- Datos del cliente estático pre-llenados -->
    <div *ngIf="clienteEncontrado" class="mt-4 p-4 border-t border-gray-300 bg-gray-100 rounded-md">
      <h3 class="text-lg font-semibold text-gray-900">Datos del Cliente</h3>
      <p><strong>DNI:</strong> {{ cliente.dni }}</p>
      <p><strong>Nombre:</strong> {{ cliente.nombre }}</p>
      <p><strong>Apellido:</strong> {{ cliente.apellido }}</p>
      <p><strong>Dirección:</strong> {{ cliente.direccion }}</p>
      <p><strong>Teléfono:</strong> {{ cliente.telefono }}</p>
      <p><strong>Correo:</strong> {{ cliente.correo }}</p>
    </div>

    <!-- Formulario para agregar cliente -->
    <div *ngIf="mostrarAgregarCliente" class="mt-4 p-4 border-t border-gray-300 bg-gray-100 rounded-md">
      <h3 class="text-lg font-semibold text-gray-900">Agregar Nuevo Cliente</h3>
      <div class="space-y-4">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div>
            <label for="dniModal" class="block text-sm font-medium text-gray-700">DNI</label>
            <input type="text" id="dniModal" [(ngModel)]="dniModal" class="mt-1 block w-full py-2 px-3 text-base border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-md shadow-sm" placeholder="DNI del Cliente" />
          </div>
          <div>
            <label for="nombre" class="block text-sm font-medium text-gray-700">Nombre</label>
            <input type="text" id="nombre" [(ngModel)]="nombre" class="mt-1 block w-full py-2 px-3 text-base border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-md shadow-sm" placeholder="Nombre del Cliente" />
          </div>
          <div>
            <label for="apellido" class="block text-sm font-medium text-gray-700">Apellido</label>
            <input type="text" id="apellido" [(ngModel)]="apellido" class="mt-1 block w-full py-2 px-3 text-base border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-md shadow-sm" placeholder="Apellido del Cliente" />
          </div>
          <div>
            <label for="direccion" class="block text-sm font-medium text-gray-700">Dirección</label>
            <input type="text" id="direccion" [(ngModel)]="direccion" class="mt-1 block w-full py-2 px-3 text-base border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-md shadow-sm" placeholder="Dirección del Cliente" />
          </div>
          <div>
            <label for="telefono" class="block text-sm font-medium text-gray-700">Teléfono</label>
            <input type="text" id="telefono" [(ngModel)]="telefono" class="mt-1 block w-full py-2 px-3 text-base border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-md shadow-sm" placeholder="Teléfono del Cliente" />
          </div>
          <div>
            <label for="correo" class="block text-sm font-medium text-gray-700">Correo</label>
            <input type="email" id="correo" [(ngModel)]="correo" class="mt-1 block w-full py-2 px-3 text-base border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-md shadow-sm" placeholder="Correo del Cliente" />
          </div>
        </div>

    <!-- Confirmación de cliente agregado -->
    <div *ngIf="mostrarConfirmacionDni" class="mt-4 p-4 border-t border-gray-300 bg-gray-100 rounded-md">
      <p class="text-green-600 font-medium">El cliente ha sido registrado correctamente.</p>
      <button (click)="confirmarDniCliente()" class="mt-2 bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700 transition duration-200 shadow-md">
        Confirmar
      </button>
    </div>
  </div>
</div>